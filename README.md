# dev
testing 

sandbox.run_module(idea_lab, "a sandbox that evolves itself")
sandbox.run_module(world_builder)
sandbox.run_module(self_debugger, "I can't think creatively")
sandbox.run_module(multi_agent_module, ["ADJJV-Agent", "Nexus", "Aegis", "Mycelium"])
sandbox.run_module(agent_interaction_loop)
sandbox.run_module(dashboard)